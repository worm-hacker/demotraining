import { CustomeHighlightDirective } from './custome-highlight.directive';

describe('CustomeHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
